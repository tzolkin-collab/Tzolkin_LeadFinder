# ─── DATABASE CONFIGURATION (POSTGRESQL) ──────────────────────────────
DATABASE_URL="postgresql://postgres:3ad3550763e84d5864a7@localhost:5432/tzolkin_lead_finder?schema=public"

# ─── SERVIDOR API CONFIG ─────────────────────────────────────────────
PORT=3001
NODE_ENV=development
JWT_SECRET="tzolkin_super_secret_jwt_key_lead_finder_2026"
CORS_ORIGIN="*"

# ─── FONTES DE DADOS PADRÃO DO SCANNER INTELIGENTE ───────────────────
# As chaves abaixo são as credenciais de desenvolvimento do projeto.
# Em produção, substitua por variáveis de ambiente reais.
GOOGLE_PLACES_API_KEY="your_google_places_api_key_here"
SERPER_API_KEY="your_serper_api_key_here"
OPENAI_API_KEY="your_openai_api_key_here"
SCRAPINGBEE_API_KEY=""
META_ADS_TOKEN=""

# ─── REDIS CONFIGURATION (OPCIONAL) ───────────────────────────────────
# Se não configurado, cache e rate-limit usam memória local
REDIS_URL="redis://localhost:6379"
REDIS_CACHE_TTL_SECONDS="604800"
