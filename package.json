{
  "name": "lead-finder",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "description": "Tzolkin Lead Finder — SaaS B2B Multi-tenant",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "@types/node": "^22.15.21"
  }
}
